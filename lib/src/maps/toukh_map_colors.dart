import 'package:flutter/material.dart';

/// Brand colors for map overlays (polylines, role-based markers).
abstract final class ToukhMapColors {
  static const Color polyline = Color(0xFF3B82F6);
  static const Color driver = Color(0xFF2563EB);
  static const Color pickup = Color(0xFF22C55E);
  static const Color destination = Color(0xFFEF4444);
}
